{
  "name": "playwright-video-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright video and trace automation patterns for debugging unstable UI flows.",
  "license": "MIT",
  "type": "commonjs",
  "scripts": {
    "test": "node --test tests/**/*.test.cjs",
    "test:video": "playwright test"
  },
  "dependencies": {},
  "devDependencies": {
    "@playwright/test": "^1.52.0"
  }
}
